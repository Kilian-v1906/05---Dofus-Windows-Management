        # header (drag + close)
        head = QHBoxLayout()
        head.setContentsMargins(0, 0, 0, 0)
        head.setSpacing(8)
        self.drag = QLabel("??", objectName="miniDrag")
        self.drag.setCursor(Qt.OpenHandCursor)
        self.btn_close = QPushButton("?", objectName="miniClose")
        self.btn_close.setFixedSize(28, 28)
        self.btn_close.clicked.connect(self._exit_to_main)
        head.addWidget(self.drag, 0, Qt.AlignLeft)
        head.addStretch()
